# Hermes Project Context

Read `AGENTS.md` first. It is the canonical project instruction file for Stashd and applies to Hermes Agent as well as Claude Code and other coding agents.

For longer Hermes-specific context, read only the relevant parts of:

```text
docs/ai/Stashd-Hermes-Agent-Reference.md
docs/ai/Hermes-Agent-Ollama-Setup.md
```

Local Hermes/Ollama reminder:

* Use the OpenAI-compatible Ollama endpoint: `http://<ollama-box-ip>:11434/v1`.
* Prefer narrow tasks touching roughly 1-4 files on the RTX 3060 12 GB setup.
* For broad/security-sensitive tasks, produce an audit/plan first rather than editing immediately.
