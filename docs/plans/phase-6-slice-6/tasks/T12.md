# T12 · Vault / media-item detail enrichment

In `vault-detail.view.php` + `vaultDetailComponent`:
- Add thumbnail, full metadata (description, provider, canonical URI, published, upstream state,
  `content_type`), **which stashes** contain it, **which broadcasts** include it, and
  download/processing status.
- Add read endpoints/relations: `GET /api/v1/items/{id}/stashes`, `GET /api/v1/items/{id}/broadcasts`
  (no back-references exist today).

**Done when:** the page answers "everything the system knows about this item", including membership.
