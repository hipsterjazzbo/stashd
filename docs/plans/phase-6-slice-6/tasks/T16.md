# T16 · Channel avatar as stash icon (consumes T2's icon_uri)

- Expose `icon_uri` on `StashResource` / `StashSummary`.
- Render it in `stashes.view.php` list (leading avatar) + `stash-detail.view.php` header
  (`h-8 w-8` rounded). Initial/placeholder fallback when absent.

**Depends on:** T2 (`icon_uri` column populated).

**Done when:** stashes with a resolved avatar show it in list + detail; placeholder otherwise.
