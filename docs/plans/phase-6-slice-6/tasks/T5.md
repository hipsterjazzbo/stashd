# T5 · YouTubeDataApiDiscoveryStrategy — full-channel commit discovery + video type

**Do:**
- New `app/Providers/YouTube/YouTubeDataApiDiscoveryStrategy.php` implementing
  `DiscoveryStrategyHandler`. Register in `YouTubeProvider::discoveryStrategies()` (`Discovery`,
  `StrategyCost::Medium`); gate `isStrategyAvailable()` on API-key presence (RSS stays `Low`/keyless).
- Page the Data API (uploads playlist / `playlistItems`) for the full channel; capture each item's
  **video type** (regular/short/live/premiere).
- Extend `app/Providers/Core/DiscoveredItem` with `?string $contentType` (RSS leaves null; Data API
  sets it). Persist on `media_items` (new `content_type` column + migration).
- **Wire intent → strategy:** preflight uses lowest-cost strategy (RSS/`fake.feed`) for the cheap
  sample; commit requests full backfill — wire the unused `JobIntent::InitialBackfill` →
  `StrategyPurpose::Discovery` best-available (`ProviderStrategySelector` already picks
  lowest-cost-available), so Data API is chosen when keyed.

**Depends on:** T6 (key source). **Files:** `YouTubeProvider.php`, `YouTubeRssDiscoveryStrategy.php`
(reference), `app/Config/YouTubeConfig.php`, `DiscoveredItem`, migration.

**Done when:** with a key, commit discovery returns >15 items each with a video type; without a key
RSS behaviour unchanged; preflight stays cheap regardless.
