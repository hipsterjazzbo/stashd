# T17 · OpenAPI spec for /api/v1

New spec file (none exists). Document existing resources + the new/changed endpoints:
- `POST /stashes`, `PATCH`/`DELETE /stashes/{id}`, `GET /stashes/{id}/delete-impact`
- `POST /stashes/{id}/inputs`, changed preflight payload (`source_title`, `source_avatar_uri`,
  `estimated_item_count`, `provider_unsupported` error)
- provider credentials endpoint
- `GET /items/{id}/stashes`, `/items/{id}/broadcasts`

Keep it within CI's release-gate reach (spec §31).

**Done when:** a valid OpenAPI document covers the v1 surface including the reframed create flow.
