# T2 · Cheap identity resolution: owner id + name + avatar + approx count

Fixes wrong-channel discovery + placeholder name + missing avatar/count in one pass.
**Keep YouTube specifics behind the provider boundary** — no `UC…`/`channelId`/handle concepts may
leak into generic preflight, `ResolvedInput`, or stash layers. New `ResolvedInput` fields are generic.

**Do:**
- `YouTubeChannelIdResolver` → return value object `ResolvedYouTubeChannel { id, title, avatarUri,
  estimatedVideoCount }` from a **single** handle-page fetch.
- **Owner id**, in order: `<link rel="canonical" .../channel/UC…>` → `og:url` `…/channel/UC…` →
  header renderer `"externalId":"UC…"`. **No first-`channelId` fallback** — if none resolve, return
  no identity (better no data than wrong data).
- Parse display **name**, **avatarUri**, **video count** (`"217 videos"` / `videosCountText`). Always
  an **estimate** ("approx. 217 videos"); abbreviated forms ("1.2K") stay approximate.
- Inject resolver into `YouTubeProvider` resolve/preview for channel handles → `ResolvedInput` whose
  `providerInputId` is the resolved `UC…` (discovery never re-resolves) + new identity fields.
  (Network-bound for handles only; video/playlist stay network-free.)
- Extend `app/Providers/ResolvedInput.php` with nullable `sourceTitle`, `sourceAvatarUri`,
  `estimatedItemCount`. **Fix the positional `ResolvedInput` reconstruction in
  `DiscoverStashInput::execute`** (else new fields drop).
- Serialize in `PreflightExecutionResult::toResultArray()`: `source_title`, `source_avatar_uri`,
  `estimated_item_count`.
- **Schema:** add denormalized `icon_uri` to `stashes`; populate from input avatar at commit. New
  dated migration (entrypoint auto-backs-up SQLite). Update `StashRecord` + `StashRepository::create`.

**Done when:** real `@handle` → its own channel id, real name, avatar, approx count in preflight;
unresolvable owner → no (not wrong) identity.
**Verify caveat:** fixture `channel_handle_page.html` always returns `UCStashdDemoCh…` — verify
against a real handle's parsed output (log id/name/avatar/count, behind `STASHD_LIVE_PROVIDER_TESTS=1`).
