# T13 · Dashboard refinement

In `dashboard.view.php` + `dashboardComponent`:
- (a) Remove Recent Jobs (debug) table.
- (b) "Recent media activity" summary from activity events, grouped per spec §29.
- (c) Per-location disk usage + total — surface `free_bytes`/`total_bytes` from `storage_locations`
  via the health payload (columns exist, currently unexposed).
- (d) Real Stashes & Vault counts.

**Done when:** no recent-jobs table; media-activity summary populated; per-location + total disk
usage shown; Stashes & Vault counts non-empty.
