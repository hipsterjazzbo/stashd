# T4 · Stash PATCH (edit) + DELETE (with shared-usage impact notice)

`StashController` has only GETs; `StashRepository` only `create/find/findBySlug/list`.

**Do:**
- Add `update(...)` + `delete(...)` to `StashRepository`.
- `PATCH /api/v1/stashes/{id}` — name/description/sync_mode/download_policy/organization_mode,
  validated via existing enums. `DELETE /api/v1/stashes/{id}`. Mirror `MediaServerController`'s
  PATCH/DELETE (reuse `notFound`/`validationError` + `ApiJson::normalizeRequest`).
- **Cascade:** delete removes stash + `stash_inputs` + `stash_items` (+ their `media_item_sources`).
  Leave deduped `media_items` and Vault originals intact by default (media-item-first).
- **Delete-impact endpoint:** `GET /api/v1/stashes/{id}/delete-impact` → for this stash's items, which
  are **shared** with other stashes (name them) vs which become **orphaned** in the Vault. Orphan
  cleanup is a separate explicit action — out of scope.
- **Frontend:** Edit + Delete on the **detail header** *and* on **stash rows in the list page**
  (`stashes.view.php`). Delete shows the impact summary, then navigates to `/stashes`. Not a blind
  `window.confirm`.

**Done when:** edit persists; delete shows shared/orphaned notice, removes the stash, leaves Vault
originals; available from list + detail; no `/login` bounce.
