# T18 · True incremental SSE streaming over RoadRunner

**Problem:** `app/System/RoadRunner/TempestPsr7Bridge.php` drains the generator and sends one burst
after `EventsController::MAX_ITERATIONS`, pinning a worker per open tab (worker-pool exhaustion,
mitigated in `573d18e`).

**Do:** implement RoadRunner `chunkSize` stream mode (`PSR7Worker::respond()`) with a
`StreamInterface` adapter that pulls from the generator, so events push incrementally instead of
pinning a worker. Touches shared runtime — verify the Dashboard / Activity / Stash-detail subscribers
+ add a server-side connection cap.

**Do this before the 6D live-update surfaces (T11 etc.).**
**Files:** `TempestPsr7Bridge.php`, `app/System/Event/EventsController.php`, `.rr.yaml`.

**Done when:** events arrive incrementally; a worker isn't pinned for the loop; open-tab count no
longer linearly consumes the pool (watch container logs / worker count).
