# T15 · Activity disclosure persists across refresh

In `activity.view.php` + `activityComponent`: the SSE handler replaces `this.events` wholesale, which
collapses any open `<details>`.

**Do:** track open-state in component state — a `Set` of expanded event ids via `x-on:toggle` +
`x-bind:open` — so re-rendering preserves which payloads are expanded.

**Done when:** expand a payload, let a background refresh fire, it stays open.
