# T14 · Animated background-activity indicator

In `main.entrypoint.css`:
- Define a `pulse` keyframe/util (none exists) + one reusable snippet: a small pulsing dot (amber when
  active; success/error colour per state). Low-animation.
- Apply to every "something is happening now" surface: page loading, in-flight buttons, active job
  rows, discovering inputs, SSE "connected". One shared pattern only.

**Verify the new Tailwind/CSS classes actually compile** (static scanning gotcha).

**Done when:** every active surface shows the dot; no other gratuitous animation.
