# Notes for Hazel

The seed is intentionally under-designed.

The first Claude prompt should make the basic shell. After that, I would resist the temptation to immediately build a real page.

Suggested next conversation after the shell:

> Good. Now Phase 2. Work only on `/design`. Give me a first pass at Stashd's visual language using representative Nuxt UI components: typography, surfaces, buttons, inputs, badges/statuses, a list row, a table fragment, an empty state, and thumbnail treatment. Don't change product pages yet. I want one coherent visual direction to react to, not exhaustive coverage. Then stop.

From there, iterate on `/design` until the vocabulary feels right. Only then lock the shell and choose the first product page.
